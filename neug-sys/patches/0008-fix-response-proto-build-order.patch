diff --git a/src/execution/CMakeLists.txt b/src/execution/CMakeLists.txt
--- a/src/execution/CMakeLists.txt
+++ b/src/execution/CMakeLists.txt
@@ -7,6 +7,6 @@ file(GLOB_RECURSE EXECUTION_SRC_FILES "${CMAKE_CURRENT_SOURCE_DIR}/common/*.cc"
     "${CMAKE_CURRENT_SOURCE_DIR}/expression/*.cc")
 
 add_library(neug_execution OBJECT ${EXECUTION_SRC_FILES})
-add_dependencies(neug_execution neug_plan_proto)
+add_dependencies(neug_execution neug_plan_proto neug_query_response_proto)
 set(ALL_OBJECT_FILES ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:neug_execution> PARENT_SCOPE)
 target_include_directories(neug_execution PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}>)
diff --git a/src/main/CMakeLists.txt b/src/main/CMakeLists.txt
--- a/src/main/CMakeLists.txt
+++ b/src/main/CMakeLists.txt
@@ -1,5 +1,5 @@
 file(GLOB_RECURSE NEUG_MAIN_SRC_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.cc"
                                         "${CMAKE_CURRENT_SOURCE_DIR}/app/*.cc")
 add_library(neug_main OBJECT ${NEUG_MAIN_SRC_FILES})
-add_dependencies(neug_main neug_plan_proto)
+add_dependencies(neug_main neug_plan_proto neug_query_response_proto)
 set(ALL_OBJECT_FILES ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:neug_main> PARENT_SCOPE)
\ No newline at end of file
diff --git a/src/utils/CMakeLists.txt b/src/utils/CMakeLists.txt
--- a/src/utils/CMakeLists.txt
+++ b/src/utils/CMakeLists.txt
@@ -89,6 +89,15 @@ compile_proto(QUERY_RESPONSE_PROTO_HDRS QUERY_RESPONSE_PROTO_SRCS
         "${QUERY_RESPONSE_PROTO_FILES}"
 )
 
+# Response proto headers are included from multiple object libraries, so the
+# generation step must be modeled explicitly to avoid clean-build races.
+add_custom_target(neug_query_response_proto
+        DEPENDS ${QUERY_RESPONSE_PROTO_HDRS} ${QUERY_RESPONSE_PROTO_SRCS})
+
+if (TARGET protobuf::protoc)
+        add_dependencies(neug_query_response_proto protobuf::protoc)
+endif()
+
 ############################################ Generate Physical Plan Proto ############################################
 set(GIE_COMPILER_PROTO_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../../proto/)
 set(GIE_COMPILER_WRITE_PROTO_DIR  ${CMAKE_CURRENT_SOURCE_DIR}/../../proto/)
@@ -137,7 +146,7 @@ list(APPEND ALL_UTILS_SRC_FILES ${QUERY_RESPONSE_PROTO_SRCS})
 add_library(neug_utils OBJECT ${ALL_UTILS_SRC_FILES})
 set(ALL_OBJECT_FILES ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:neug_utils> $<TARGET_OBJECTS:neug_plan_proto> PARENT_SCOPE)
 
-add_dependencies(neug_utils neug_plan_proto)
+add_dependencies(neug_utils neug_plan_proto neug_query_response_proto)
 
 target_include_directories(neug_utils PRIVATE ${CMAKE_CURRENT_BINARY_DIR}/neug/generated/proto/plan)
 
